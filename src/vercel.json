{
    "builds": [
        {
            "src": "api/**/*.js",
            "use": "@now/node",
            "config": {
                "external": [
                    "axios"
                ]
            }
        }
    ]
}